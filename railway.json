{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "(php artisan config:clear || true) && (php artisan cache:clear || true) && (php artisan route:clear || true) && (php artisan view:clear || true) && php artisan migrate --force && php artisan db:seed --force && (php artisan passport:keys || true) && php artisan serve --host=0.0.0.0 --port=$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

